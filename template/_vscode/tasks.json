{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm: dev (frontend)",
            "type": "shell",
            "command": "npm run dev",
            "options": {
                "cwd": "${workspaceFolder:💻 Frontend}"
            },
            "problemMatcher": []
        }
    ]
}
